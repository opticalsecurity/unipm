export enum PackageManager {
  BUN = "bun",
  PNPM = "pnpm",
  YARN = "yarn",
  NPM = "npm",
}

export const PackageManagers = [
  PackageManager.BUN,
  PackageManager.PNPM,
  PackageManager.YARN,
  PackageManager.NPM,
];
